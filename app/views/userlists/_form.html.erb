<%= form_with(model: userlist, local: true) do |form| %>
  <% if userlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userlist.errors.count, "error") %> prohibited this userlist from being saved:</h2>

      <ul>
      <% userlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!--
  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>
-->

  <div class="field">
    <%= form.label :topic %>
    <%= form.text_field :topic %>
  </div>

  <div class="field">
    <%= form.label :receipients %>
    <%= form.text_field :receipients %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
