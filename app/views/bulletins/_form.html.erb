<%= form_with(model: bulletin, local: true) do |form| %>
  <% if bulletin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bulletin.errors.count, "error") %> prohibited this bulletin from being saved:</h2>

      <ul>
      <% bulletin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :from %>
    <%= form.text_field :from %>
  </div>

  <!--
  <div class="field">
    <%# <%= form.label "Select a Topic for Userlists" %> 
    <%#<% options = options_from_collection_for_select(@userlists, 'id', 'topic') %>
    <%#<%= form.select(:userlists[:topic] , options) %>
  </div>
  -->
  <div class="form-group">
    <%= form.label "Select a topic" %>
    <%= form.select :topic, @userlists.map { |t| [t.topic, t.receipients]}, {}, class: "form-control"  %>
  </div>

<!--
  <div class="field">
  <%#  <%= form.label :to %>
    <%# <%= form.text_field :to %>
    <ul>
   <%#  <%= @userlists.each do |ul| %>
    <li>
     <%#  <%= ul.receipients %>
    <li>
    <%# <% end %>
    </ul>
  </div>
-->
  <div class="field">
    <%= form.label :to %>
    <%= form.text_field :to %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label "Send Bulletin to Users Now?" %> 
    <%= form.select(:flag, options_for_select([["True", 1] , ["False", 0]])) %>
    <%= puts(bulletin.flag)  %>
  </div>

  <div class="actions">
    <%= form.submit "Send Bulletin"%>
  </div>
<% end %>
